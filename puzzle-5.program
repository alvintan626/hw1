//puzzle 5

function repeat(direction,total) {
  var count = 0;
  while (count < total) {
    direction();
    count = count + 1;
  }
}

repeat(right,2)

if (getColor() == "blue") {
  down();
  repeat(right,2);
} else {
  up();
  repeat(right,2);
}

if (getColor() == "blue") {
  down();
  repeat(right,2);
} else {
  up();
  repeat(right,2);
}

if (getColor() == "blue") {
  down();
  repeat(right,2);
} else {
  up();
  repeat(right,2);
}

repeat(right,2)
